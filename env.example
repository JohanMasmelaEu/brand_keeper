# ============================================
# VARIABLES NECESARIAS (Requeridas para que el proyecto funcione)
# ============================================

# Supabase Configuration
# Obtén estos valores desde tu proyecto en https://supabase.com/dashboard
# Settings > API > Project URL
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Settings > API > Project API keys > anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Service Role Key
# IMPORTANTE: Solo para uso en server-side, NUNCA exponer en cliente
# Settings > API > Project API keys > service_role secret
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Next.js App URL
# URL base de la aplicación (para desarrollo usar localhost:3000)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# VARIABLES OPCIONALES (No necesarias para el funcionamiento básico)
# ============================================

# Variables de Supabase (alternativas sin NEXT_PUBLIC_)
# Estas son equivalentes a las versiones con NEXT_PUBLIC_ pero para uso en servidor
# Settings > API
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# JWT Secret (para validación de tokens personalizada)
# Settings > API > JWT Secret
SUPABASE_JWT_SECRET=your_supabase_jwt_secret

# Variables de PostgreSQL (para conexión directa a la base de datos)
# Útiles para migraciones, scripts o herramientas externas
# Settings > Database > Connection string > Connection pooling
POSTGRES_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Settings > Database > Connection string > Direct connection
POSTGRES_URL_NON_POOLING=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Para Prisma ORM (si se usa en el futuro)
POSTGRES_PRISMA_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres?pgbouncer=true&connect_timeout=15

# Resend API Key (para envío de correos electrónicos)
# Obtén tu API key en: https://resend.com/api-keys
# Gratis hasta 3,000 correos/mes
RESEND_API_KEY=your_resend_api_key

# Email remitente para Resend
# Debe ser un dominio verificado en Resend
# Ejemplo: Brand Keeper <noreply@tudominio.com>
RESEND_FROM_EMAIL=Brand Keeper <noreply@brandkeeper.com>

# Variables individuales de PostgreSQL (extraídas de la connection string)
POSTGRES_USER=postgres
POSTGRES_HOST=db.[YOUR-PROJECT-REF].supabase.co
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DATABASE=postgres

